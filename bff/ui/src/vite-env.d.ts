/// <reference types="vite/client" />
// Ambient declarations for Vite's non-TypeScript module imports (the
// `import "./styles.css"` in `main.tsx`), so `tsc -p bff/ui` typechecks the
// SPA without loosening `strict`/`verbatimModuleSyntax`.
