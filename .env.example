# Optional. Local Wrangler secrets belong in .dev.vars, never in source control.
PAGESPEED_API_KEY=

# Required for every /mcp request. Use a long random value and never commit .dev.vars.
MCP_AUTH_TOKEN=
